import React from "react";

function MainAreaTitle(props) {
    return (
        <div className="main-area-title">
            <span>{props.title}</span>
        </div>
    );
}

export default MainAreaTitle;